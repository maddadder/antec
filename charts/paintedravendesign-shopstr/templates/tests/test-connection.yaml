apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "paintedravendesign-shopstr.fullname" . }}-test-connection"
  labels:
    {{- include "paintedravendesign-shopstr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "paintedravendesign-shopstr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
