apiVersion: v1
kind: Service
metadata:
  name: {{ include "tnt86-nostr-rs-relay.fullname" . }}
  labels:
    {{- include "tnt86-nostr-rs-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "tnt86-nostr-rs-relay.selectorLabels" . | nindent 4 }}
