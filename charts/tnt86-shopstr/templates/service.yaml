apiVersion: v1
kind: Service
metadata:
  name: {{ include "tnt86-shopstr.fullname" . }}
  labels:
    {{- include "tnt86-shopstr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tnt86-shopstr.selectorLabels" . | nindent 4 }}
