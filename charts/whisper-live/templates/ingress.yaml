apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-whisper-live
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - whisper-live.dankapps.app
    secretName: dankapps-app-gcloud-dns-tls
  rules:
  - host: whisper-live.dankapps.app
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: whisper-live
            port:
              number: 9090