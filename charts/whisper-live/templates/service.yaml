apiVersion: v1
kind: Service
metadata:
  name: {{ include "whisper-live.fullname" . }}
  labels:
    {{- include "whisper-live.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whisper-live.selectorLabels" . | nindent 4 }}
