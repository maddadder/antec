apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whisper-live.fullname" . }}-test-connection"
  labels:
    {{- include "whisper-live.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whisper-live.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
